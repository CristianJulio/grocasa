import styled from "styled-components";

export const PropertiesContainerStyled = styled.ul`
  h4 {
    color: var(--clr-text-2);
    font-size: var(--fs-medium);
  }

  ul {
    list-style-type: none;

    li {
      color: var(--clr-text);
      font-size: var(--fs-normal);
    }
  }
`